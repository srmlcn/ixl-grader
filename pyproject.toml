[project]
name = "ixl-grader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"

dependencies = ["pandas>=2.3.2", "streamlit>=1.28.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
